---
import MenuContainer from "../MenuContainer.astro";
import BlogCardContainer from "../BlogCardContainer.astro";
import CardMenu from "../CardMenu.astro";
import {
  HEADER_LEARN_CARD,
  getBlogContentList,
  categoryMap,
  LEARN_CARD_LIST,
} from "../../../constant";

const { blogDescrip, blogImgs, posts } =
  ((await getBlogContentList(HEADER_LEARN_CARD)) as any) || {};
---

<MenuContainer>
  <CardMenu cardData={LEARN_CARD_LIST} slot="left" />
  <BlogCardContainer
    blogData={{ categoryMap, blogDescrip, blogImgs, posts }}
    slot="right"
  />
</MenuContainer>
